// 1. Definir la función `repetirOperacion`
function repetirOperacion(fun, num) {
  // 2. Ejecutar `fun` el número de veces que indica `num`
}

// 3. Definir la función `multiplicar` que multiplica por 2

// 4. Llamar a `repetirOperacion` con `multiplicar` y 5 como argumentos
